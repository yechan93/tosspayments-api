spring:
  datasource:
    url: jdbc:mysql://localhost:3306/settlement_db?useSSL=false&allowPublicKeyRetrieval=true&characterEncoding=UTF-8
    username: root
    password: root1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

  batch:
    job:
      enabled: false        # 앱 시작 시 배치 자동실행 방지
    jdbc:
      initialize-schema: always  # 배치 메타 테이블 자동 생성

toss:
  payments:
    secret-key: test_sk_d46qopOB899Xp7pm6lod8ZmM75y0  # 토스 개발자센터에서 발급
    base-url: https://api.tosspayments.com

logging:
  level:
    com.tosspayments.api: DEBUG
    org.springframework.batch: DEBUG